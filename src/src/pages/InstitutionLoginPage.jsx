import { useState } from "react";
import { useLocation, useNavigate } from "react-router-dom";
import { ArrowLeft, Building2, KeyRound, LogIn, UserRound } from "lucide-react";
import { getLastInstitutionCode, loginInstitutionStudent } from "../utils/institutionStorage";
import "../styles/access.css";
export default function InstitutionLoginPage(){const navigate=useNavigate();const location=useLocation();const savedCode=location.state?.accessCode||getLastInstitutionCode();const [form,setForm]=useState({accessCode:savedCode,studentCode:"",pin:""});const [error,setError]=useState("");const [loading,setLoading]=useState(false);const set=(key,value)=>setForm(current=>({...current,[key]:value}));const submit=async(event)=>{event.preventDefault();setError("");setLoading(true);try{await loginInstitutionStudent(form);navigate("/home",{replace:true});}catch(e){setError(e.message||"Código de alumno o PIN incorrecto.");}finally{setLoading(false);}};return <main className="access-screen institution-student-screen"><button className="institution-back" onClick={()=>navigate("/institution-access")}><ArrowLeft/> Volver</button><section className="institution-student-card institution-login-card"><div className="institution-student-icon"><LogIn/></div><span className="profiles-eyebrow">ALUMNO REGISTRADO</span><h1>Entra a EduPlay</h1><form className="institution-student-form" onSubmit={submit}><label><span><Building2/> Código del colegio</span><input value={form.accessCode} onChange={e=>set("accessCode",e.target.value.toUpperCase())} readOnly={Boolean(savedCode)} autoComplete="organization"/></label><label><span><UserRound/> Código del alumno</span><input autoFocus value={form.studentCode} onChange={e=>set("studentCode",e.target.value.toUpperCase())} autoComplete="username"/></label><label><span><KeyRound/> PIN</span><input type="password" inputMode="numeric" value={form.pin} onChange={e=>set("pin",e.target.value.replace(/\D/g,""))} maxLength={12} autoComplete="current-password"/></label>{error&&<div className="form-error premium-form-error">{error}</div>}<button className="primary-access-button" disabled={loading||!form.accessCode.trim()||!form.studentCode.trim()||!form.pin}>{loading?"Ingresando...":"Ingresar"}</button></form></section></main>;}
